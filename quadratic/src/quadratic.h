/**
	This is our implementation of a quadratic equation solver
	using methods of the extreme programming.
	Required things include
		unit testing
		Doxygen compatable commenting
		lots of modularization
		
*/
///Used to determine one of the roots
float QuadraticPlus(float, float, float);

///Used to determine the other root
float QuadraticMinus(float, float, float);

///used to get input values
float GetAValue(char letter);
